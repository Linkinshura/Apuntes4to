# Javascript

### Indicar Verdadero o Falso

document.getElementById() devuelve un único elemento.

En JavaScript, const permite reasignar el valor de una variable.

addEventListener() se usa para escuchar eventos como clics o teclas presionadas.

innerHTML permite cambiar el contenido HTML de un elemento.

### Completa el método para seleccionar todos los elementos con la clase item

document.________________("item");


### Completa la línea de código que muestra un mensaje en la consola

____________("Hola Mundo");


### Completa el evento que detecta un clic

element.addEventListener("_______", function() {
    console.log("Click detectado");
});

### ¿Qué método se utiliza para crear un nuevo elemento HTML en el DOM?

document.newElement()

document.createElement()

document.addElement()

document.makeElement()

### Explica con tus palabras qué es el DOM y para qué se utiliza en JavaScript.


### Completa el siguiente código para que, cuando el usuario haga clic en el botón con id "btn", se muestre el mensaje "Botón presionado" en la consola

const boton = document.getElementById("btn");

boton._________________("click", function() {
    console.log("Botón presionado");
});
