# Unidad 1 - Laboratorio de Algoritmos de Estructura de Datos

### Algoritmo:

Un algoritmo es una serie de pasos ordenados con un proposito, como una receta o una palabra en terminos mas elaborados

## Estructura de Datos:

Es el orden y distribucion de un elemennto especifico

### Basicos:

-Bit

-Byte

-Palabra

### Simple:

-Numeros enteros(INT)

-Numeros Reales(FLOAT,DOUBLE)

-Alfanumericos(CHAR)

-Logicos(BOOLEAN)

-Punteros(CHAR*)

### Simple Derivada:

#### Variables:

Es un dato que no se rige por un valor especifico, pudiendo variar este dentro de su tipo de dato libremente

#### Constante:

Es un dato que al momennto de ser creado se le puede asignar un valor pero una vez que tiene valor no se puede cambiar este


### Complejas:

-Vectores o Arrays

-Matrices

-Arboles

-Grafos

-Bases de Datos

## Analisis del problema

Esta etapa esta hecha con el proposito de identificar que se pide y como realizarlo, esta dividido en 3 secciones:

### Entrada:

Aca estan los datos que se nos dan de base para resolver el problema

### Proceso:

Se crea un calculo o solucion para resolver el problema

### Salida:

Se muestra el resultado del problema mostrando su solucion

### Ejemplo:

Realizar un programa que lea la edad y un numero y que luego diga en que año va a cumplir esos años 

| Entrada | Proceso | Salida |
|---------|---------|--------|
| Edad, año, num | año_aprox = Año + num | año_aprox |

## Diseño del algoritmo:

En esta etapa plantearemos como va a realizar el programa la tarea deseada, planteando una secuencia de pasos en formato algoritmica,

Utilizando diagramas de flujos y pseudocodigo

### Pseudocodigo:

Es codigo pero utilizando palabras en el propio idioma hablado(Español en nuestro caso)

En vez de escribir PRINTF escribimos IMPRIMIR, se añade INICIO y FIN al principio y final del bloque de codigo

Tabla:

| Codigo | Pseudocodigo | 
|---------|---------|
| PRINTF | IMPRIMIR |
| SCANF  | LEER |


### [Diagrama de Flujos](https://youtu.be/zuCHSKLIibo?si=FE1GGvdoTRygepav)

## Compilacion y Ejecucion:

En esta etapa, mediante un compilador, el programa fuente es traducido a lenguaje máquina.

Si el programa no tiene errores, se obtendrá como resultado el código objeto, que se podrá ejecutar.

## Depuracion y Verificacion:

La depuración es el proceso de encontrar los errores del programa y corregir o eliminar dichos errores.

### Errores de Sintaxis:

Se producen por un uso incorrecto de las reglas del lenguaje de programación.

la computadora no entiende la instrucción.

### Errores Logicos:

Se producen en la etapa de diseño del algoritmo.

Son errores en el planteo de la lógica del programa.

### Errores en tiempo de ejecucion:

Se deben a instrucciones que la computadora entiende pero que no puede realizar.

Por ejemplo división por cero.

## Documentacion y Mantenimiento:

La documentación de un programa consta de las descripciones de los pasos para dar en el proceso de resolución de un problema.

La importancia de la documentacion debe ser destacada por su decisiva influencia en el producto final.

Programaspobremente documentados son difíciles de leer dificiles de depurar y casi imposibles de mantener y modificar.

La documentación de un programa puede ser interna y externa. 

La documentación interna es la contenida entre las línea de comentarios.

La documentación externa incluye el análisis, diagramas de flujo y/o pseudocódigos, manuales de usuarios con instrucciones para ejecutar el programa y para interpretar los resultados.


